export interface SidebarLink {
  imgURL: string;
  route: string;
  label: string;
}

export interface ISettingsCard {
  id: number;
  icon: string;
  label: string;
}
export interface IDuaCardLinks {
  id: number;
  icon: string;
  label: string;
}
